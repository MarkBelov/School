#include <iostream>

using namespace std;

int main()
{
    int n, b, d;
    cin >> n;
    int *a = new int[n];
    int c[n];
    for (int i = 0; i < n; i++) {
        cin >> b;
        a[i] = b;
    }
    cin >> d;
    for (int i = 0; i < n; i++) {
        if (i + (d % n) < n) {
            c[i + (d % n)] = a[i];
        } else {
        c[i + (d % n) - n] = a[i];
        }
    }
      for (int i = 0; i < n; i++) {
        cout << c[i] << " ";
    }
    return 0;
}
